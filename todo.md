# Ajustes Finais (Logos e Mobile)

- [ ] **ImpactSection.tsx:**
    - Remover `hover:grayscale-0` e `hover:opacity-100`.
    - Definir `grayscale` e `opacity-100` (ou valor alto fixo) permanentemente.
- [ ] **WhatSection.tsx:**
    - Verificar responsividade dos gráficos (Recharts).
    - Garantir que `ResponsiveContainer` esteja sendo usado corretamente.
    - Adicionar `min-height` ou ajustes de CSS para evitar colapso em telas pequenas.
