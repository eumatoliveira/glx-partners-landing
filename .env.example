# ═══════════════════════════════════════════════════════════════
# GLX Partners — Environment Configuration
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# ── Core ──
NODE_ENV=development
PORT=3000

# ── Security ──
# REQUIRED in production. Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=

# ── Database ──
# Leave empty to use in-memory fallback for local development
DATABASE_URL=

# ── Bootstrap Users (for local dev only) ──
# Admin account
BOOTSTRAP_ADMIN_EMAIL=admin@example.com
BOOTSTRAP_ADMIN_PASSWORD=change_this_admin_password

# Demo users (format: email|password|plan|name separated by semicolons)
BOOTSTRAP_DEMO_USERS=demo@example.com|Demo123!|essencial|Demo User

# Legacy test client bootstrap
BOOTSTRAP_TEST_CLIENT_EMAILS=client@example.com
BOOTSTRAP_TEST_CLIENT_PASSWORD=change_this_client_password
BOOTSTRAP_TEST_CLIENT_NAME=Cliente de Teste GLX

# ── Optional Integrations ──
OAUTH_SERVER_URL=
VITE_APP_ID=
OWNER_OPEN_ID=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=

# ── Notification Settings ──
VITE_TEST_NOTIFICATION_EMAILS=
